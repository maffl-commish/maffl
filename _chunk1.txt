    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .dues-table th:last-child,
  .dues-table td:last-child {
    text-align: right;
    font-variant-numeric: tabular-nums;
    font-weight: 700;
    white-space: nowrap;
  }
  .dues-table tbody tr:last-child td { border-bottom: 0; }

  /* ---------- Section heading ---------- */
  .section-head {
    border-bottom: 1px solid var(--border);
    padding-bottom: 8px;
    margin-bottom: 12px;
  }
  .section-head h2 {
    font-family: var(--display);
    font-weight: 700;
    font-size: 13px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin: 0;
  }

  /* ---------- Card grid (springboard) ---------- */
  .card-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-auto-rows: 1fr; /* every row matches the tallest cell â†’ uniform cards */
    gap: 12px;
  }

  .section-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    min-height: 96px;
    height: 100%;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 14px;
    box-shadow: 0 1px 3px rgba(26, 46, 80, 0.06);
    transition: transform 0.15s ease, border-color 0.15s ease, box-shadow 0.15s ease;
  }
  .section-card:hover,
  .section-card:focus-visible {
    transform: translateY(-1px);
    border-color: var(--navy);
    box-shadow: 0 6px 16px var(--navy-shadow);
    outline: none;
  }
  .section-card:active { transform: translateY(0); }

  /* The League grid â€” historical/archive data. Softer than Quick Access:
     light-gray tint, no border or shadow at rest, pops to white on hover. */
  .card-grid--archive .section-card {
    background: #f8f9fa;
    border-color: transparent;
    box-shadow: none;
  }
  .card-grid--archive .section-card:hover,
  .card-grid--archive .section-card:focus-visible {
    background: var(--surface);
    border-color: var(--navy);
    box-shadow: 0 6px 16px var(--navy-shadow);
  }

  .card-icon {
    font-size: 24px;
    line-height: 1;
    margin-bottom: 7px;
  }
  .card-title {
    font-family: var(--display);
    font-weight: 700;
    font-size: 14px;
    color: var(--navy);
    margin: 0 0 4px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    line-height: 1.1;
  }
  .card-tag {
    font-family: var(--body);
    font-weight: 400;
    font-size: 11px;
    color: var(--muted);
    margin: 0;
    line-height: 1.3;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* ---------- What's New â€” Hero Carousel ---------- */
  .carousel { position: relative; }

  /* The swipeable track: horizontal scroll + native scroll-snap so each
     slide locks into place on mobile. Scrollbar hidden for a clean hero look. */
  .carousel-track {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    border-radius: 10px;
    scrollbar-width: none;
  }
  .carousel-track::-webkit-scrollbar { display: none; }

  /* Each slide fills the full width of the track and snaps to the start edge. */
  .slide {
